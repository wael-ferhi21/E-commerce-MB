{% extends 'base.html.twig' %}

{% block title %}Mes adresses{% endblock %}

{% block content %}
<a href="{{path('app_add_adresse')}}" class="btn btn-info btn-sm float-right" >Ajouter une adresse </a><br/>
<h1>Mes adresses</h1>
C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.<br/>
<a href="{{path('app_account')}}">Retour. </a><br/>
<hr>
{% if  app.user.adresses | length == 0 %}
<p class="text-center">
       Vous n'avez pas encore ajouter d'adresses dans votre compte client. Pour en ajouter une veulliez <a href="{{path('app_add_adresse')}}"> Cliquer ici </a>
</p>
{% else %}
<div class="row">
{% for address in app.user.adresses %}     
<div class="col-md-4">
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{ address.nomadr }}</h5>
    <p class="card-text">
    {{ address.adresse }} <br/>
    {{ address.codepostal}} - {{ address.cite}} -  {{ address.pays}} <br/>
    </p>
    <a href="{{path('app_edit_adresse', {'id': address.id})}}" >Modifier</a> | <a href="{{path('app_delete_adresse', {'id': address.id})}}" >Supprimer</a>
  </div>
</div>
</div>
{% endfor %}
</div>
{% endif %}
{% endblock %}