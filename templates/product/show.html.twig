{% extends 'base.html.twig' %}

{% block title %}{{produit.nom}} {% endblock %}

{% block content %}

<div class="row" >
   <div class="col-md-3">
      <img src= "/upload/image/produit/{{produit.image}}" alt="{{produit.nom}}" calss="img fluid">
   </div>
   <div class="col-md-7"  >
      <h3>{{produit.nom}}</h3>
      <span class="produit-page-prix"> {{(produit.prix / 100)|number_format('2', ',', '.') }} TND</span>
      <hr>
      <p>
      {{produit.proddescr}}
      </p>
      <a href="{{path('app_cart_add_Produits',{'id':produit.id})}}" class="btn btn-primary"> Ajouter au panier </a>
   </div>
</div>
<hr> 
<h3>Nos meilleur ventes</h3>
<p>Découvrez les articles les plus vendus</p>

<div class="row">
         {% for produit in produits %}
           <div class="col-md-2"> 
               {% include 'product/single_produit.html.twig' %}  
           </div>
         {% endfor %}
        </div>



{% endblock %}
